# Movie-Review-API
Name: Yeabtsega Samuel
section:B
ID Number: RDCS-088-24B
Project Description:A simple REST API built with Node.js using only the `http` and `fs` modules. The project performs CRUD operations on movie data stored in a JSON file, following REST principles and proper HTTP methods.